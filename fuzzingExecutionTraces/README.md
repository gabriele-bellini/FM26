# Fuzzer Runs: Simple and Extended Scenarios

This folder includes two fuzzer runs. The first corresponds to the simplest configuration—3 users and a single protocol execution. The second illustrates the extensibility of the model and approach, running with 4 users and two protocol initiations per run.